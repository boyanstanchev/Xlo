export interface AdCreateInterface {
  title: string,
  condition: string,
  category: string,
  subCategory: string,
  featured: boolean,
  model: string,
  price: number,
  imageUrl: string,
  creator: string
}
